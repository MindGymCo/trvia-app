//search funtionality
//add note card
//delete note card on the card
//save note card -- local storage?


function Notecard(term,definition){
    this.term = term;
    this.definition = definition;
    
    
    };
    
    renderNoteCard(){
        showCard();
        deleteCard();
    };
    function showCard(){
    
    };
    
    function deleteCard(){
        if (event.target.innerHTML == "imgsource")
        let 
    };
    
    Notecard.prototype.addNoteCard(term,definition){
    let newNotecard = new Notecard(term,definition)
    
    };